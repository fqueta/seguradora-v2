# Application Configuration
VITE_APP_NAME=My App
VITE_DEBUG=false

# API Configuration
# Development base API (WITHOUT version). If multi-tenant, you can use {tenant_id}.
# Examples:
# VITE_TENANT_API_URL=http://{tenant_id}.localhost:8000/api
# VITE_TENANT_API_URL=http://localhost:8000/api
VITE_TENANT_API_URL=http://{tenant_id}.localhost:8000/api

# API version (suffix only). Do NOT include '/api' here.
# Examples:
# VITE_API_VERSION=/v1
# VITE_API_VERSION=v1
VITE_API_VERSION=/v1

# Optional fallback: legacy single-tenant base URL (used if VITE_TENANT_API_URL is not set)
# VITE_API_URL=http://localhost:8000/api

# For production - domain for multi-tenant setup (not required if using VITE_TENANT_API_URL)
# VITE_API_DOMAIN=example.com

# Optional: Supabase Configuration (if using Supabase)
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# reCAPTCHA v3 (frontend)
# Site key obtained from Google reCAPTCHA Admin (v3). Required for InviteEnroll.
VITE_RECAPTCHA_SITE_KEY=